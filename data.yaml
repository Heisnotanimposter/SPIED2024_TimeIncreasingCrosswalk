import requests

esp32_ip = "http://192.168.x.x"  # Replace with your ESP32 IP address

# Turn LED on
requests.get(f"{esp32_ip}/control?var=statePin16&val=on")

# Turn LED off
requests.get(f"{esp32_ip}/control?var=statePin16&val=off")
